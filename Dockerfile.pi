FROM node:24.16.0-bookworm-slim

# @lat: [[wiki#Sandbox image#Search tooling]]
RUN apt-get update \
  && apt-get install -y --no-install-recommends bash ca-certificates git ripgrep fd-find \
  && ln -s /usr/bin/fdfind /usr/local/bin/fd \
  && rm -rf /var/lib/apt/lists/*
# @lat: [[wiki#Sandbox image#Pi agent install]]
RUN npm install -g --ignore-scripts @earendil-works/pi-coding-agent@0.78.1

# Pre-create the agent state dir owned by the non-root runtime user so the
# root-initialised named volume mounted there stays writable under --user 1000
# and --read-only.
# @lat: [[wiki#Sandbox image#Writable agent-state directory]]
RUN mkdir -p /home/node/.pi/agent && chown -R node:node /home/node

WORKDIR /workspace
RUN chown node:node /workspace
USER node
ENTRYPOINT ["pi"]